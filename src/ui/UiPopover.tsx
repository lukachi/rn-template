import { Popover as HNPopover } from 'heroui-native'

const Popover = HNPopover
const PopoverTrigger = HNPopover.Trigger
const PopoverPortal = HNPopover.Portal
const PopoverOverlay = HNPopover.Overlay
const PopoverContent = HNPopover.Content
const PopoverArrow = HNPopover.Arrow
const PopoverClose = HNPopover.Close
const PopoverTitle = HNPopover.Title
const PopoverDescription = HNPopover.Description

export {
  Popover as UiPopover,
  PopoverArrow as UiPopoverArrow,
  PopoverClose as UiPopoverClose,
  PopoverContent as UiPopoverContent,
  PopoverDescription as UiPopoverDescription,
  PopoverOverlay as UiPopoverOverlay,
  PopoverPortal as UiPopoverPortal,
  PopoverTitle as UiPopoverTitle,
  PopoverTrigger as UiPopoverTrigger,
}
