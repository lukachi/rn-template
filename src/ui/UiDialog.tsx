import { Dialog as HNDialog } from 'heroui-native'

const Dialog = HNDialog
const DialogClose = HNDialog.Close
const DialogContent = HNDialog.Content
const DialogOverlay = HNDialog.Overlay
const DialogPortal = HNDialog.Portal
const DialogTrigger = HNDialog.Trigger
const DialogTitle = HNDialog.Title
const DialogDescription = HNDialog.Description

export {
  Dialog as UiDialog,
  DialogClose as UiDialogClose,
  DialogContent as UiDialogContent,
  DialogDescription as UiDialogDescription,
  DialogOverlay as UiDialogOverlay,
  DialogPortal as UiDialogPortal,
  DialogTitle as UiDialogTitle,
  DialogTrigger as UiDialogTrigger,
}
