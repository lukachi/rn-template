import { TextField as HNTextField } from 'heroui-native'

const TextField = HNTextField

const TextFieldLabel = HNTextField.Label
const TextFieldInput = HNTextField.Input
const TextFieldDescription = HNTextField.Description
const TextFieldErrorMessage = HNTextField.ErrorMessage

export {
  TextField as UiTextField,
  TextFieldDescription as UiTextFieldDescription,
  TextFieldErrorMessage as UiTextFieldErrorMessage,
  TextFieldInput as UiTextFieldInput,
  TextFieldLabel as UiTextFieldLabel,
}
